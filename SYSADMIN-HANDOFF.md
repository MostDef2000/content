# SYSADMIN-HANDOFF v1

Передаётся сисадмин-агенту для удалённой диагностики и подготовки развёртывания
стека локальной генерации AI-модели (Valery). Это **read-only** хэндоф:
никаких изменений без отдельного подтверждения в следующем шаге.

## objective
Исследовать свободное место на диске и предусловия на GPU- и VPS-серверах,
подтвердить готовность к развёртыванию стека (ComfyUI + manager + Caddy).
Записать фактические замеры в ответном хэндофе.

## hosts
- GPU: `10.123.239.102` (ZeroTier; RTX 5070 12 ГБ, драйвер 595.91, CUDA 13.2,
  Docker 29.8 + Compose v5.5.1). Используйте свой SSH-доступ к этому узлу.
- VPS: `77.105.142.206` (root; Caddy + домен `ui.example.com`). Свой SSH-доступ.
- Алиасы из `~/.ssh/config` у оркестратора недоступны — подставьте свои.

## mode
read-only (только замеры и проверки; без установки/записи/публикации)

## forbidden
- Не публиковать порты `8188`/`8000` в интернет; не менять фаервол наружу.
- Не скачивать модели и не запускать `docker compose up` в этом хэндофе.
- Не выводить и не пересылать `HF_TOKEN`, пароли, приватные ключи, токены сессий.
- Не трогать базовый YOLO-воркер без согласования (делит GPU).

## context
- Репозиторий: `MostDef2000/content` (private), уже запушен, содержит `compose.yaml`,
  `management/`, `deploy/Caddyfile`, `scripts/`, `AUDIT.md`, `README.md`.
- Чек-лист развёртывания: issue #1.
- Оценка весов (для планирования): flux1-dev-fp8 ~12 ГБ, kontext fp8 ~12 ГБ,
  t5xxl_fp8 ~5 ГБ, clip_l ~0.5 ГБ, ae ~0.3 ГБ, ai-toolkit venv ~5 ГБ → ~35 ГБ суммарно.
- Локально (workspace) свободно 726 ГБ; репозиторий 628 КБ (только код).

## required_evidence
GPU-сервер (`10.123.239.102`):
- **Полная инвентаризация дисков** (не только `/`):
  - `lsblk -f` — все блочные устройства, файловые системы, точки монтирования, размеры
  - `findmnt -D` или `df -h` — все смонтированные тома и свободное место по каждому
  - есть ли второй диск (data) помимо системного 69 ГБ? если да — точка монтирования и свободно ГБ
- `du -sh /var/lib/docker` и оценка под модели (~35 ГБ) + датасет/посты/LoRA
- `nvidia-smi --query-gpu=memory.total,memory.used --format=csv` (занято YOLO ~1.6 ГБ)
- `docker info | grep -i runtime` (nvidia runtime активен)
- куда планируется `runtime/` (модели ~35 ГБ, dataset, posts, lora/output): на системный диск
  или на data-диск, если он есть

VPS (`77.105.142.206`):
- `df -h /` (достаточно под Caddy + логи — нужно <1 ГБ)
- `which caddy && caddy version`
- наличие домена `ui.example.com` и его A-записи на этот IP
- доступность `10.123.239.102:8000` по ZeroTier (ping / nc -vz)

## proposed_changes (план, без исполнения)
- Если на GPU есть второй data-диск: смонтировать его (например, `/data`), и направить
  `runtime/` туда — либо bind-mount в compose (`/data/valery/runtime:/opt/ComfyUI/...`),
  либо symlink `instagram-model/runtime -> /data/valery/runtime`. Это бережёт системный
  диск (69 ГБ) и даёт место под датасет, посты и чекпоинты LoRA.
- Если второго диска нет: подтвердить, что 69 ГБ хватает с запасом под модели + venv + датасет
  (несколько LoRA-версий и кэш латентов могут быстро съесть место — нужен бэкап/ротация).

Отчёт: вернуть `status`, `commands_run` (точные), `evidence`,
`proposed_changes` (только план, без исполнения), `risks`, `unresolved`,
`kb_updated` (ссылка на JOURNAL/INFRASTRUCTURE).
